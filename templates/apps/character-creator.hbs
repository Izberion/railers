<form class="character-creator flexcol" onsubmit="event.preventDefault();">
  <header class="creator-head">
    <div class="remaining-points">{{localize "RAILERS.apps.character.pointsRemaining"}}: {{pointsRemaining}} / {{data.pointsTotal}}</div>
      <div class="mutation">
        <label>
        <input
            type="checkbox"
            data-action="toggleMutation"
            {{checked data.useMutation}}
        />
        {{localize "RAILERS.apps.character.mutationRoll"}} (+25 {{localize "RAILERS.apps.character.points"}})
        </label>
        {{#if data.mutation}}
        <div>{{localize "RAILERS.apps.character.mutation"}}: {{data.mutation.name}}</div>
        {{/if}}
    </div>
  </header>

  <section class="attributes">
    <h3>{{localize "RAILERS.apps.character.attributes"}} (5 {{localize "RAILERS.apps.character.points"}})</h3>
    <div class="attribute-list">
      {{#each data.attributes as |attribute a|}}
        <div class="attribute">
          <label>{{localize attribute.label}}</label>
          <input
            type="number"
            data-type="attribute"
            data-attribute="{{a}}"
            value="{{attribute.value}}"
            min="0"
          />
        </div>
      {{/each}}
    </div>
  </section>

  <section class="skills">
    <h3>{{localize "RAILERS.apps.character.skills"}} (3 {{localize "RAILERS.apps.character.points"}})</h3>
    {{#each data.attributes as |attribute a|}}
      <div class="att-group flexrow">
        <div class="attribute-label">{{localize attribute.label}}</div>
        <div class="skill-list">
          {{#each attribute.skills as |skill s|}}
            <div class="skill">
              <label>{{localize skill.label}}</label>
              <input
                type="number"
                data-type="skill"
                data-attribute="{{a}}"
                data-skill="{{s}}"
                value="{{skill.value}}"
                min="0"
                max="{{attribute.value}}"
              />
            </div>
          {{/each}}
        </div>
      </div>
    {{/each}}
  </section>

  <section class="stats">
    <h3>{{localize "RAILERS.apps.character.stats"}}</h3>
    <div class="stat-list">
      <div class="stat">
        <label>{{localize "RAILERS.Actor.base.FIELDS.hitpoints.max"}} (2 {{localize "RAILERS.apps.character.points"}} / +2)</label>
        <input
          type="number"
          data-type="hitpoints"
          value="{{data.hitpoints.max}}"
          min="12"
          step="2"
        />
      </div>
      <div class="stat">
        <label>{{localize "RAILERS.Actor.base.FIELDS.nerve.max"}} (2 {{localize "RAILERS.apps.character.points"}} / +4)</label>
        <input
          type="number"
          data-type="nerve"
          value="{{data.nerve.max}}"
          min="20"
          step="4"
        />
      </div>
      <div class="stat">
        <label>{{localize "RAILERS.Item.base.FIELDS.therms"}} (1 {{localize "RAILERS.apps.character.points"}} / +5)</label>
        <input
          type="number"
          class="therms"
          data-type="therms"
          value="{{data.therms}}"
          min="200"
          step="5"
        />
      </div>
    </div>
  </section>

  <footer>
    <button type="button" data-action="createCharacter">{{localize "RAILERS.apps.character.create"}}</button>
  </footer>
</form>