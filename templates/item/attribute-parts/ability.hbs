{{! Attributes Tab }}
<section
  class='tab attributes {{tab.cssClass}}'
  data-group='primary'
  data-tab='attributes'
>
    <div class="tab attributes" data-group="primary" data-tab="attributes">
        <div class="resource">
            <label class="resource-label">{{localize "RAILERS.Action"}}</label>
            <select name="system.action">
                {{selectOptions actionTypeOptions selected=system.action localize=true}}
            </select>
        </div>
    </div>
</section>